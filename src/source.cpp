#include "header.h"

    vector<int> twoSum(vector<int>& numbers, int target) {
        vector<int> sum;
        for(auto it = numbers.begin(); it != numbers.end(); ++it)
        {
            for(auto jt = numbers.begin() + 1; it != numbers.end(); ++jt)
            {
                
            }
        }
    }

    // void reverseString(vector<char>& s) 
    // {
    //     /*
    //         Этапы: 
    //             1. Проверить строку на пустоту
    //             2. Создать вторую строку
    //             3.    
        
        
    //     */

    //     vector<int> n;
        
        
    //     short i = 0;
    //     char c_start = 0;
    //     char c_end = 0;
        
    //     if(nullptr == &s)
    //     {
    //         return nullptr;
    //     }
    //     while(i != ((s.size() / 2) + 1))
    //     {
    //         c_start = s[i];
    //         c_end = s[s.size() - i];
    //         s[i] = c_end;
    //         s[s.size() - i] = c_start;
    //         ++i;
    //     }
    }